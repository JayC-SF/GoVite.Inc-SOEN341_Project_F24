export const AuthRoutes = {
    isLoggedIn: "/api/isloggedin",
    logout: "/api/logout",
    login: "/api/login",
    signup:"/api/sign-up"
} as const

export const StudentsRoutes = {
    students: "/api/students",
} as const